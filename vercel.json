{
  "buildCommand": "cd plasma-sim && npm install && npm run build",
  "outputDirectory": "plasma-sim/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
